---
description: is helpful for creating new files, folders, understanding the structure of the app
globs: src/**/*.{js,ts,jsx,tsx}
---
use next with src, app router, next js 15, typescript, tailwind and shadcn@latest components.

db is neon with drizzle-orm, dialect is posgresql and we use drizzle-kit push to update schema.

put db stuff inside new src/db folder

make sure to use server components when possible, optimise for performance.

